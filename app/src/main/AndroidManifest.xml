<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.delightful">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Delightful">
        <activity android:name=".Invoice" />
        <activity android:name=".Amount_Calculatorbeta" />
        <activity android:name=".Pizza" />
        <activity android:name=".Pasta" />
        <activity android:name=".Sandviches" />
        <activity android:name=".Redirect" />
        <activity android:name=".Chinese" />
        <activity android:name=".Main_menu" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>